<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Books Books Books</title>
    <link href="styles/reset.css" rel="stylesheet"/>
    <link href="styles/base.css" rel="stylesheet"/>
    <link href="styles/layout.css" rel="stylesheet"/> 
    <link href="styles/modules.css" rel="stylesheet"/> 
    <script
    src="https://code.jquery.com/jquery-3.5.1.min.js"
    integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
    crossorigin="anonymous"></script>
</head>
<body>
    <main>
        <% let counter = 0; %>
        <% searchResults.forEach(bookObj => { %>
        <div>
            <li id="image"><img src=<%= bookObj.image %> alt="picture of book"></li>
            <li id="title" class="centeredWords"><%= bookObj.title %></li>
            <li id="authors"><%= bookObj.authors %></li>
            <li id="description"><%= bookObj.description %></li>
            <form id="createbookshelf" action="/books" method="post">
                <button type="submit">To The BookShelf!</button>
                <input hidden id="image" name="image" value="<%= bookObj.image %>"> <!-- it's *just* hidden. no type. no.type! -->
                <input type="hidden" id="title" name="title" class="centeredWords" value="<%= bookObj.title %>"> <!--we assume the addtl "class" type throws it off the "easy" note typed "hidden" property/attribute.-->
                <input type="hidden" id="authors" name="authors" value="<%= bookObj.authors %>">
                <input hidden id="description" name="description" value="<%= bookObj.description %>">
                <input hidden id="isbn" name="isbn" value="<%= bookObj.isbn %>">
                <input hidden id="thisnumber" name="arraylocation" value="<%= counter %>"> <!-- for it to be passed into response, what is in the form must be an input. -->
                <% counter += 1 %>
            </form>
        </div>
        <% }) %>
    </main>
    
    <script src="js/app.js"></script>

</body>
</html>